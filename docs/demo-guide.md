# Judge Demo Guide

Open the dashboard and select `LOT-2026-041`. Select `CMP-LATENT-017`, the seeded latent-risk component. The investigation view shows its measured 0h value under the 50 µA specification maximum and marks static screening PASS. The lot baseline then shows the large robust deviation from peers.

Continue through the 0h-to-24h trajectory, the 168h forecast, residual interval, safety boundary, margin, risk decomposition, and suggested action. Open an investigation, select a human QA outcome, add rationale, and record it. The UI confirms that the decision is stored in the audit trail. All data is synthetic demonstration data and is not presented as ISRO production data.
